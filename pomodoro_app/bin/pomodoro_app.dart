import 'package:flutter/material.dart';
import 'package:pomodoro_app/pomodoro_app.dart' as app;

void main() {
  // Este arquivo bin não é necessário para Flutter
  print('Este é um app Flutter. Use "flutter run" no terminal.');
  print('O arquivo principal está em lib/main.dart');
  
  // Se quiser executar o app a partir daqui (não recomendado):
  // app.main();
}